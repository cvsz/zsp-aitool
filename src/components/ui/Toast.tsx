export function Toast({ message, type = "info" }: { message: string; type?: "success" | "error" | "info" }) { const tone = type === "success" ? "bg-emerald-600" : type === "error" ? "bg-red-600" : "bg-slate-800"; return <div className={`fixed right-4 top-4 z-50 rounded-lg px-4 py-2 text-sm text-white ${tone}`}>{message}</div>; }
